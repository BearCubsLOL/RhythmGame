using UnityEngine;

public class BeatPlayer : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        if (GetComponent<BeatScroller>().gameActive == true)
        {
            //if (Input.GetKey("D"))
            //{

            //}
        }
    }
}
